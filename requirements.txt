# Core runtime dependencies for QuantDesk
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.40
PySide6>=6.7.0
requests>=2.31.0
pyarrow>=15.0.0  # for parquet read/write
# Backtesting / TA (optional but used if present)
backtrader>=1.9.78.123
TA-Lib>=0.4.28; platform_system != 'Windows'  # wheels on manylinux; on Windows may require manual install
# If TA-Lib wheel not available, comment the above and optionally install 'ta' (Pandas TA) as alternative
# pandas_market_calendars optional for future trading day logic
pandas_market_calendars>=4.3.0
# Machine learning (currently optional placeholders in codebase)
scikit-learn>=1.4.0
xgboost>=2.0.0
lightgbm>=4.0.0
joblib>=1.3.0
# Deep learning (optional)
torch>=2.2.0
# Additional utilities (some code paths reference them optionally)
matplotlib>=3.8.0
pytest>=8.0.0  # for running tests folder if needed
pyfolio @ git+https://github.com/quantopian/pyfolio.git#egg=pyfolio  # optional analytics (legacy)
# If you use AlphaVantage / Polygon API keys no extra installs needed beyond requests
